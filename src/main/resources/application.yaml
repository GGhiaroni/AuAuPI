spring:
  application:
    name: AuAuPi

  datasource:
    url: jdbc:postgresql://localhost:5431/AuAuPI
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration